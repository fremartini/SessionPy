type <int> as n;
local protocol DynServer at Sy(role Sy,role Rob) {
    offer to Rob {
        @neg;
        Num(n) from Rob;
        Num(n) to Rob;
        End;
    } or {
        @add;
        Num(n) from Rob;
        Num(n) from Rob;
        Num(n) to Rob;
        End;
    }
}