type <str> as Message;
type <float> as Price;

local protocol BuyerSeller at Seller(role Alice, role Bob, role Seller) {
    Prompt(Message) from Alice;
    Answer(Message) to Alice;

    choice from Bob {
        Response(Message) from Bob;
        End;
    } or {
        End;
    }
}