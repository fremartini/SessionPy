local protocol BuyerSeller at Bob(role Alice, role Bob, role Seller) {
    Quotation(Price) from Seller;
    Quotation(Price) from Alice;

    offer to Seller {
        Response(Message) to Seller;
        End;
    } or {
        End;
    }
}