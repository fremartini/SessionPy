from channel import Channel, Branch
from sessiontype import *

# ch = Channel[Send[List[int], Send[Tuple[str, int], Send[Dict[int, float], End]]]]()

# my_dict = {2: 2.718, 3: 3.142}
# ch.send([1,2,3])
# ch.send(('hi', 42))
# ch.send(my_dict)

ys : Dict[str, int] = {"a": 123, "b": 14, "c": 5}
