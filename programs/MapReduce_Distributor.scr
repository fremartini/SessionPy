type <List[int]> as workload;
local protocol MapReduce at Distributor(role Distributor,role c1,role c2,role c3) {
Work(workload) to c1;
Work(workload) to c2;
Work(workload) to c3;
Res(int) from c1;
Res(int) from c2;
Res(int) from c3;
}