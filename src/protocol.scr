global protocol HelloWorld(role Me, role World) {
	str from Me to World;
	str from World to Me;

	choice at World {
		str from World to Me;
                do HelloWorld(Me, World);
	} or {
		int from World to Me;
	}
}